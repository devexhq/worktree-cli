name: test
description: Run pytest suite in-place without sandbox isolation
use_sandbox: false

steps:
  - id: run-tests
    name: Run tests
    type: command
    command: uv run inv test
    timeout_seconds: 300
    on_failure: abort
